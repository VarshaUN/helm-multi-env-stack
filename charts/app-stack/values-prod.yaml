frontend:
  replicaCount: 3
  resources:
    requests:
      cpu: 150m
      memory: 128Mi
    limits:
      cpu: 300m
      memory: 256Mi

backend:
  replicaCount: 3
  resources:
    requests:
      cpu: 200m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

postgresql:
  primary:
    persistence:
      enabled: true
      size: 5Gi
  auth:
    username: appuser
    password: prodpassword
    database: appdb